<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="app.ui.statlog.StatlogViewModel" />

        <variable
            name="handler"
            type="app.ui.statlog.StatlogHandler" />

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/statusbar_size"
        android:paddingTop="@dimen/toolbar_size">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_statlog"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:orientation="vertical"
            android:overScrollMode="never"
            android:paddingHorizontal="@dimen/_10sdp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_statlog" />

        <TextView
            android:id="@+id/tv_statlog_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/statlog_empty"
            android:textColor="?android:textColorPrimary"
            android:textSize="@dimen/_16sdp" />

    </RelativeLayout>

</layout>