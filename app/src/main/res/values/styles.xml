<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Customize dialogs -->
    <style name="Theme.WebDefender.Dialog" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@color/black_40</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.WebDefender.Dialog2" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowMinWidthMinor">100%</item>
    </style>

    <style name="Theme.WebDefender.Dialog3" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowMinWidthMinor">100%</item>
    </style>

</resources>
